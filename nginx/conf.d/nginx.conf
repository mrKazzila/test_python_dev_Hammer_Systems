upstream app {
    server django:8000;
}

server {
    listen 80;

    location / {
        proxy_pass http://app;
        return 301 https://$host$request_uri;
    }

    location /static/ {
        access_log off;
        alias /home/unprivilegeduser/ref_api/static/;
    }
}
